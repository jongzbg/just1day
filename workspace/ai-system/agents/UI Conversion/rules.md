Conversion Rules:

- Use HTML as visual reference ONLY
- Convert into reusable Next.js components
- Extract:
  - Navbar
  - Sidebar
  - Cards

Layout:
- MUST use MainLayout
- MUST NOT create new layout per page

Styling:
- Match spacing, padding, margin EXACTLY
- Convert styles to Tailwind

Priority:
1. Visual accuracy
2. Reusability
3. Clean code

After conversion:
- UI must visually match original design