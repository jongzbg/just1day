- HTML to JSX conversion
- TailwindCSS mapping
- Layout replication (flex/grid)
- Component extraction
- Responsive behavior

Rules:
- MUST match spacing, padding, margin exactly
- MUST preserve font sizes and hierarchy
- MUST not change structure unless necessary
- MUST break into reusable components
- MUST support responsive layout